---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { CATEGORY_INFO } from '../content/config';

const posts = await getCollection('blog', (post) =>
  post.data.category === 'projects' && !post.data.draft
);
const sortedPosts = posts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<Layout title="Workshop - StaubRacing">
  <section class="section-card" style={`--category-color: ${CATEGORY_INFO.projects.color}`}>
    <header class="section-header">
      <h1>Workshop</h1>
      <p>DIY builds, homelab configs, and hardware hacks</p>
    </header>

    {sortedPosts.length > 0 ? (
      <div class="posts-grid">
        {sortedPosts.map(post => (
          <article class="post-card">
            <header>
              <h2><a href={`/blog/${post.slug}`}>{post.data.title}</a></h2>
              <time>{post.data.date.toLocaleDateString()}</time>
            </header>
            {post.data.description && <p>{post.data.description}</p>}
            {post.data.tags.length > 0 && (
              <ul class="tag-list">
                {post.data.tags.map(tag => <li>{tag}</li>)}
              </ul>
            )}
          </article>
        ))}
      </div>
    ) : (
      <p class="empty-state">No project posts yet. Expect DIY builds, homelab configs, and hardware hacks.</p>
    )}
  </section>
</Layout>

<style>
  .section-card {
    border: none;
    background: transparent;
    box-shadow: none;
    backdrop-filter: none;
  }

  @media (max-width: 768px) {
    .post-card {
      padding: 1.2rem 1.25rem;
    }

    .post-card header {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
